name: Whitespace

on:
  pull_request:
    branches: [ 'main', 'release-*' ]

permissions: read-all

jobs:

  whitespace:
    name: Check Whitespace
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: chainguard-dev/actions/trailing-space@dd4524afd480a43ead9543a08be1c5004ee8b0a7 # v1.0.7
        if: ${{ always() }}

      - uses: chainguard-dev/actions/eof-newline@dd4524afd480a43ead9543a08be1c5004ee8b0a7 # v1.0.7
        if: ${{ always() }}
