name: Whitespace

on:
  pull_request:
    branches: [ 'main', 'release-*' ]

permissions: read-all

jobs:

  whitespace:
    name: Check Whitespace
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - uses: chainguard-dev/actions/trailing-space@1df2b5598adffbfe72a3abe9243d73e338bbb006 # v1.4.10
        if: ${{ always() }}

      - uses: chainguard-dev/actions/eof-newline@1df2b5598adffbfe72a3abe9243d73e338bbb006 # v1.4.10
        if: ${{ always() }}
