apiVersion: policy.sigstore.dev/v1beta1
kind: ClusterImagePolicy
metadata:
  name: cip-sigaas
spec:
  images:
  - glob: "**hectorj2f/**"
  authorities:
  - keyless:
      url: https://cbtkngtewlpetwcbrxqv-sigstore.hectorj2f.dev
      trustRootRef: my-sigstore-keys
      identities:
        - issuer: 'https://auth.chainguard.dev/'
          subject: 'hector@chainguard.dev'
      insecureIgnoreSCT: true
    rfc3161timestamp:
      trustRootRef: my-sigstore-keys
    #source:
    #- signaturePullSecrets:
    #  - name: regcred

